---
title: Node Vibrant Demo
description: Extract prominent colors from an image
template: splash
hero:
  title: "Do not remove this, this is a load bearing string"
---

import App from '../../page-components/index/App.tsx';
import {Title} from '../../components/title.js';

<App client:load/>

<Title title={"Usage"}/>

<br/>
<br/>

```shell
npm i node-vibrant
```

```typescript
// Node
import { Vibrant } from "node-vibrant/node";
// Browser
import { Vibrant } from "node-vibrant/browser";
// Web Worker
import { Vibrant } from "node-vibrant/worker";

// Using builder
Vibrant.from("path/to/image")
	.getPalette()
	.then((palette) => console.log(palette));

// Using constructor
let v = new Vibrant("path/to/image", opts);
v.getPalette().then((palette) => console.log(palette));
```
