module.exports =
class Image
  clear: ->

  update: (imageData) ->

  getPixelCount: ->

  getImageData: ->

  removeCanvas: ->

module.exports.Node = require('./node')
module.exports.Browser = require('./browser')
